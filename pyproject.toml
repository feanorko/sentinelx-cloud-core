[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sentinelx-cloud-core"
version = "0.1.0"
description = "SentinelX agent: connects a Linux server to sentinelx-cloud-hub via WebSocket"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "Pensados" }]

dependencies = [
    "websockets>=12.0",
    "pydantic>=2.6,<3",
    "pyyaml>=6.0",
    "structlog>=24.1",
    "sentinelx-cloud-protocol @ git+https://github.com/pensados/sentinelx-cloud-protocol.git@v1.0.0",
]

[project.scripts]
sentinelx-cloud-core = "sentinelx_core.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/sentinelx_core"]

[tool.ruff]
line-length = 100
target-version = "py311"
